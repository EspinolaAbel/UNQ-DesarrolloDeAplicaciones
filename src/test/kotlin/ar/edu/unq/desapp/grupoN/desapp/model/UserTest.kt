package ar.edu.unq.desapp.grupoN.desapp.model

import org.junit.jupiter.api.Test
import kotlin.test.assertEquals

class UserTest {

    /*
    @Test
    fun user_creates_new_sell_operation() {
        val user = User()
        val price = 19_000.5
        val quantity = 1.0
        //val symbol = Symbol.BTCUSDT

        val createdOperation = Operation(null, user, /*symbol, */price, quantity, OperationType.SELL)

        assertEquals(user, createdOperation.user)
        //assertEquals(symbol, createdOperation.symbol)
        assertEquals(price, createdOperation.price)
        assertEquals(quantity, createdOperation.quantity)
        assertEquals(OperationType.SELL, createdOperation.operation)
    }
    */

    /*
    @Test
    fun user_creates_new_buy_operation() {
        val user = User()
        val price = 19_000.5
        val quantity = 1.0
        val symbol = Symbol.BTCUSDT

        val createdOperation = user.newBuyOperation(symbol, price, quantity)

        assertEquals(user, createdOperation.user)
        assertEquals(symbol, createdOperation.symbol)
        assertEquals(price, createdOperation.price)
        assertEquals(quantity, createdOperation.quantity)
        assertEquals(OperationType.BUY, createdOperation.operation)
    }
    */

}